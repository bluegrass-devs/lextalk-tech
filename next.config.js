/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/lextalk-tech',
    assetPrefix: '/lextalk-tech',
    images: {
        domains: [
            "placekitten.com"
        ]
    }

}

module.exports = nextConfig