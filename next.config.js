/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["placekitten.com", "res.cloudinary.com"],
    unoptimized: true,
  },
  output: "export",
};

module.exports = nextConfig;

module.exports = nextConfig;
